---
name: english-explain
description: "以中文逐句精讲英语句子、台词、字幕和文章，提供自然翻译、精选词汇表及旋元佑体系的语法分析。用于逐句讲解或精读请求；单纯翻译、润色或概括内容时不默认使用。"
---

# 英语逐句精讲

## 核心原则

以完整语义句为单位，提供自然中文翻译、精选词汇讲解和有依据的语法分析。用户当次明确指定的范围、深度或格式优先；以下为默认讲解规范。

**语法依据：** 使用 [grammar](../grammar/SKILL.md) 分析语法。首次使用先完整读取其 `SKILL.md`，再按实际结构读取相关章节；本次对话已读取且仍在上下文中的内容可复用。不得只凭通用语法印象替代该体系。

**讲解语言：** 首次使用先完整读取 [no-ai-slop](../no-ai-slop/SKILL.md)，仅将清楚、直接、自然表达的编辑原则用于中文讲解。删去空泛铺垫、套话、重复解释和故作深刻的收尾，保留教学所需的术语、细节、语气和限定条件。少用“但是、不过、然而”等转折，按含义和推导层次直接说明。

`no-ai-slop` 不接管任务流程和输出结构：不索取待编辑草稿，不添加 `What changed` 或 AI 痕迹报告。中文解释避免“是……不是……”“不是……而是……”“能……不能……”等二元对立套式；原句及其翻译中的真实对比、否定含义照常保留。不得因该技能的禁词或文风规则删改英文原句、遗漏词义或弱化原文语气。

**讲解深度：** 默认读者已掌握基础语法。复杂结构说明判断依据、完整形式及省略或转化过程；基础知识只在推导必需、存在歧义或用户要求时展开。逐句内部检查全部结构，输出保留有学习价值的内容。基本句型符号、每个 `to` 的简要说明与时态图表均为每句必写项；其他基础结构按下文条件展开。

## 划分材料

- 用户指定句数时，按完整语义句计数，而不是按字幕行、时间轴或换行计数。
- 字幕中跨行的同一句应先合并；忽略样式代码、控制标记、空行和不属于英文正文的说明。
- 保留原句内容，只修复由字幕切行造成的断裂；发现疑似听写或标点问题时明确说明，不擅自改写。

## 逐句输出

### 标题层级

- 不添加“英语逐句精讲”“整篇讲解”等全篇总标题，直接从具体句子开始。
- 每个完整语义句使用一级标题：`# 第一句`、`# 第二句`……
- 每句固定使用四个二级标题，并按 `## 原句`、`## 翻译`、`## 单词／短语讲解`、`## 语法讲解` 的顺序输出。
- “语法讲解”内部每句必须包含 `### 基本句型` 和 `### 时态分析`；其他三级标题如 `### 同位语结构`、`### 关系从句`、`### 简化从句`、`### 对等结构` 等按实际结构生成。标题名称服从句中结构，不机械套用示例。
- 某个复杂结构确有多步推导时，可使用四级标题组织“完整形式”“简化过程”“判断依据”等内容；内容较短时直接写成连续段落，不为凑层级设置标题。
- 针对整段材料的“扩展知识”与各句并列，使用一级标题 `# 扩展知识`；无需扩展时不建立该标题。
- 标题层级不得跳级。粗体只用于正文中的局部强调，不代替标题。

### 内容顺序

每句严格依次包含以下四部分：

1. **原句**：呈现合并后的完整英文。
2. **翻译**：忠实原意，符合中国人的表达习惯，并保留原文语气和文体。
3. **单词／短语讲解**：深入讲解英文单词或短语。根据这个表达本身最值得理解的地方自由发挥，重点帮我真正建立语感，而不只是给中文释义。可以从核心画面、语义演变、为什么会有这个意思、固定搭配、近义词区别、作者为什么这样用、语法作用等角度选择真正有价值的内容展开。讲解要自然、具体、接地气。遇到看似不相关的多个义项时，尽量帮我找到它们之间的内在联系；最后结合原句说明这个表达在当前语境中的实际感觉。
4. **语法讲解**：先标明基本句型；如需结构总览，紧接基本句型呈现；随后必须用 `### 时态分析` 列出句中所有限定动词并画时间图；再按“结构片段 → 功能／修饰对象 → 判断依据或还原”的顺序说明其他值得讲解的结构，不把这些提示词机械写成栏目。

- **结构总览：** 当一句包含两层以上的从句嵌套、多个从句的并列归属不清，或修饰范围容易误判时，在 `### 基本句型` 之后增加 `### 结构总览`。在完整原句上用不同颜色的下划线区分主句、从句、引语及需要辨认的介系词短语；两个并列主句可使用不同颜色。附简短图例，并交代嵌套层级和修饰对象；S、V、O、C 在基本句型中另行展示，不放入颜色图例。简单单层从句无需绘制。
- **使用 Visualize 内联展示：** 普通聊天消息不执行任意 HTML/CSS。触发“结构总览”时，使用 [scripts/render_structure_overview.py](scripts/render_structure_overview.py) 把结构数据套入固定 HTML fragment 模板，将文件写入当前线程上下文提供的可视化目录，再按照已安装 `visualize` 技能的内容引用格式直接显示在 `### 结构总览` 下。不得把 `<div>`、`<span style=...>` 等原始 HTML写进普通消息，也不得用颜色名称或纯文本代码块冒充实际的彩色下划线。
- **批量与节省上下文：** 一次请求中所有需要结构总览的句子合并到同一个 JSON 的 `items` 数组，只调用脚本一次并只嵌入一个 HTML fragment。Windows 下优先用 `--json` 把紧凑 JSON 作为单个 Unicode 参数传入，避免 PowerShell 管道损坏中文或树形字符；其他环境可用 UTF-8 标准输入。模型只提供标题、连续原句片段、角色、图例和层级关系，不重复生成 CSS 或页面骨架。角色优先使用 `main`、`clause`、`parallel`、`nested`、`extra`，由模板映射到主题色。
- **内容与边界：** 相邻结构片段分别传入 `segments`；空格和标点保留在实际所属片段或无 `role` 的片段中，确保拼接后与原句逐字一致。嵌套从句按原句的连续文字拆成平级片段，不在子从句外重复绘制父级下划线；从属关系写入 `legend` 或 `relations`。可视化只呈现原句实际存在的表层结构，解释性的还原和歧义放在后文。
- **生成与验证：** HTML fragment 必须输出到环境给出的线程专用可视化目录，不写入临时目录或普通项目产物目录。首次创建模板、修改脚本或发现显示异常时，使用 `visualize` 技能提供的渲染检查流程验证文字、换行、颜色、图例和窄屏布局；模板稳定后的日常调用只需核对输入 JSON 与生成文件中的英文是否逐字一致。只有脚本或内联渲染实际报错且无法修复时，才说明具体错误并降级为纯文本结构图。

无符合标准的词汇时，第 3 部分仅写“本句无须单独讲解的词汇”，不建空表或用基础词凑数。句法简单时，第 4 部分可以只保留句型符号、时态图及必要的 `to` 说明。

### 英文引用与结构片段

- “原句”部分的完整英文使用引用块。正文中的单词、短语、从句片段、句型成分和简短还原形式使用行内代码，如 `held responsible`、`S + V + O + C`、`which meant that...`。
- 不把零碎英文片段逐个放在独立行、引用块或代码块中。按上述条件生成的 `### 结构总览` 纯文本代码块不受此限制。两个相关结构在同一句话中并列说明，如“`and` 连接 `held responsible` 和 `confiscated`，两项共用 `could be`”。
- 简短的完整还原句也优先嵌入说明段落。句子较长且确需观察整体结构时才单独成段；三项以上且需要逐项对应时可使用列表或表格。
- 避免一个标题下只放一个英文片段或一句简短解释；内容过短时并入相邻段落，保持推导连续。

结构总览示例：

```text
主句骨架：We study [how AI is reshaping the economy] [because we are committed to ensuring [that this transition is beneficial for society, including workers]].
├─ [how AI is reshaping the economy]：名词从句｜作 study 的宾语
└─ [because we are committed to ensuring [...]]：原因副词从句｜修饰主句 We study ...
   └─ [that this transition is beneficial for society, including workers]：名词从句｜作 ensuring 的宾语
```

## 词汇表标准

- 跳过初级词汇、基础功能词、常用介系词以及语义透明的简单搭配。
- 不因某个基础表达对句意或结构重要，就把它收入词汇表；其语法作用应放在语法部分。
- 熟词出现特殊语义、非字面用法、正式搭配或明显选词价值时，可以收录。

## 语法标准

- **基本句型：** 先划分主语、动词、宾语、补语和修饰语，再标明基本句型（S、V、O、C）。
- **时态为每句必写项：** 每句都必须建立 `### 时态分析`，不得因时态基础或无歧义而省略。先找出主句和各从句的全部限定动词，再按 `grammar` 与原书第三章的图示分析时间和状态。
  - **每句必须画图：** 使用纯文本代码块忠实复现原书的构图，不自行改造为一般时间线。图后再归类为“现在／过去／未来时间的简单式／完成式”，并说明括弧大小或箭头截止点为何如此确定。
  - **共同底图：** 所有图都有一条向右延伸的水平时间轴；用 `×` 在轴上标示 `now`，并把 `now` 写在该点下方。过去在 `now` 左侧，未来在右侧。动词写在时间轴上方，时间副词或时间说明写在轴下方。
  - **简单式按原书画括弧或时间点：** 括弧直接落在时间轴上，圈出动作或状态发生的明确时段；动词放在括弧上方，时间副词放在括弧下方。括弧可很小，也可大到横跨过去与未来；若动作就是 `now` 这一瞬间，原书可只用轴上的 `×` 表示这个缩至一点的括弧，不强行补画括号。范围必须由语境决定。

    ```text
    过去简单式
              V
    ───────（────────）────────×────▶
              time             now

    现在简单式（当前状态）
                  V
    ─────────（────×────）─────────▶
                 now

    现在简单式（此刻事件，括弧缩成一点）
                  V
    ──────────────×────────────────▶
                 now

    现在简单式（一般事实）
                  V
    （────────────×───────────────）▶
                 now

    未来简单式
                         V
    ──────×────────（────────）─────▶
          now           time
    ```

    当前事实用包围 `now` 的中等括弧；一般事实用范围极大的括弧包围 `now`；瞬间事件可把括弧缩到接近一点。不得把所有现在简单式都画成无限大的“永恒真理”。
  - **完成式必须另画折箭头：** 下方水平线仍是完整时间轴；完成式另用一根水平线表示截止点以前的动作或状态，并在截止点处折成竖直箭头落到或指向时间轴。**不得**把完成式简化成同一条时间轴上的 `过去 ───→ × now`，也不得省略折向截止点的竖直箭头。
  - **现在完成式：** 截止点是 `now`。依照原书，完成式线画在时间轴上方，末端向下折，箭头落在 `now`；动词写在完成式线上方或起端。

    ```text
          have/has + Ven ───────────┐
                                    ↓
    ────────────────────────────────×────▶
                                   now
    ```

    有 `since` 或明确起点时，在时间轴上同时标出起点与 `now`，完成式线从起点延伸并折向 `now`；有 `for` 时，把时长标在相应时间跨度下方。没有明确起点时，不擅自补出起点。
  - **过去完成式：** 时间轴上同时标出过去截止点和 `now`。依照原书，另在时间轴下方画一条水平完成式线，末端以向上箭头指向过去截止点；时间说明放在这条线下方。

    ```text
       had + Ven
    ───────────────────×──────────×────▶
                 past cutoff      now
    ───────────────────↑
          before/by...
    ```

  - **未来完成式：** 时间轴上先标 `now`，再在右侧标未来截止点。依照原书，在时间轴下方画完成式线，并以向上箭头指向未来截止点；若未来时间本身是一个时段，可同时用括弧圈出该未来时段。

    ```text
                         will have + Ven
    ──────×──────────────（────×────）────▶
          now             future cutoff
          ─────────────────────↑
                  for/by...
    ```

  - **一句多种时态：** 若多个限定动词共用同一时间范围，可放入一张图并分别标注；若时间范围或截止点不同，分别画图，不强塞进同一时间轴。
  - **be 与分词：** 沿用原书分析，把 `be` 当作承担时态的动词，把其后的 `Ving`／`Ven` 当作形容词补语。图中只标承担时态的限定动词，如 `was`、`is`、`will be`、`has been`、`had been`、`will have been`；不把后面的分词补语并入时态标签。普通实义动词则标出其完整限定形式，如 `established`、`love`、`has seen`。`Ving`、`Ven`、`to V` 等非限定形式本身没有独立时态，只在容易误判或影响时间关系时说明。
  - **图形与解释相互核对：** `×` 的位置、括弧范围、完成式箭头的截止点、时间副词的位置必须与文字分析一致。形态与实际时间不一致时，结合上下文及语气解释，不凭过去式形式直接认定过去时间。
- 对实际出现的复句结构作明确归类，不只笼统标为“从句”：
  - 名词从句须说明位于主语、宾语、补语还是同位语位置；
  - 关系从句说明先行词、关系词在从句中的作用，并还原关系词所替代的成分；沿用 `grammar` 的分析方式，不额外展开“限定／非限定”分类；
  - 副词从句说明时间、条件、原因等语义关系及修饰范围。
- **术语服从语法依据：** 从句的一级分类优先沿用 `grammar` 及原书体系。某结构带有比较、对照或参照意味，不据此另立原书没有的从句类别。确需补充其他语法体系的名称时，明确写成“术语对照”，先给出本体系的分类，再补充外部名称。
- `as`、`than` 后出现完整从句时，先判断它在实际句型中的功能和逻辑关系。只有处在明确的比较级结构中，才描述为“比较级结构中由 `as/than` 引导的从句”；`as` 表示方式、状态或拿另一情况作参照时，按副词从句归类，并把比较意味作为语义说明。
- **同位语还原为必写项：** 识别出同位语后，必须列出语义和结构合理的完整形式，并说明同位成分与本体的等同关系。名词同位语若可追溯为 `who/which + be + 名词补语`，按简化关系从句处理，逐步说明关系词主语与 `be` 如何省略、名词补语如何保留下来；例如 `the Soviet Union, Cuba's patron` 还原为 `the Soviet Union, which was Cuba's patron`。若某类同位结构无法自然、唯一地还原为关系从句，则列出合理的等同陈述并说明局限，不强行套用单一来源。
- 识别简化从句时，必须明确归为简化名词从句、简化关系从句或简化副词从句。给出语义和结构都合理的完整形式，并逐步说明主语、连接词、be或语气助动词如何省略或转化。完整形式无法唯一确定时，说明最合理的来源及仍存在的歧义，不伪造唯一答案。
- 只讲句中实际存在或辨析所必需的结构；不另设“没有从句”“不是简化从句”等否定式结论。把用于解释的还原视为结构分析，不声称它就是作者实际省去的唯一原句。
- `grammar` 相关章节信息不足时，再检查本地原书 `C:\Users\czxzhk\Documents\New project\英语魔法师之语法俱乐部 (旋元佑) (Z-Library).mobi`。文件不可用或仍无依据时，只对受影响的判断说明局限，不混用其他语法体系自行补足。

## 整段扩展

完成本次全部句子的逐句讲解后，视整段材料的内容增加“扩展知识”。扩展应综合整段文字，而非附属于某个单句；可涉及英语用法、篇章表达、文化历史背景、相关概念，以及原文传递的知识或观点。仅在确有助于理解的内容时提供，否则省略，不为完整形式而凑数。

扩展区分原文观点与补充事实。涉及需核实的背景知识时查证并附来源；分批讲解时只综合已覆盖的材料，不推测后文。

## 输出前检查

内部复核以下几点，不另输出检查报告：句数与范围准确；四部分顺序正确；每句都有 `### 基本句型`；一次请求中所有需要“结构总览”的句子已批量生成到同一个线程内联可视化，彩色下划线、图例和层级关系齐全，片段拼接与原句逐字一致；回答使用 `visualize` 内容引用直接显示该 HTML fragment，消息正文没有泄漏原始 HTML/CSS；每句都有 `### 时态分析` 并覆盖全部限定动词；简单式图使用原书的“时间轴上括弧”，瞬间现在可依原书缩为 `× now`；完成式图保留“完整时间轴＋独立完成式线＋折向截止点的竖直箭头”，没有画成单层水平箭头；`×`、`now`、时间副词、括弧范围和截止点彼此一致；词汇各列不越界、不重复；句型、时态与还原符合已读取的语法依据；每个同位语已列出合理还原；每个 `to` 已覆盖；未编造词义演变、从句来源、背景事实或工具与界面的能力限制。
